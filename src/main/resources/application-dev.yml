spring:
  datasource:
    url: jdbc:mariadb://mariadb:3306/prodcat_db
    username: root
    password: douae
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate.dialect: org.hibernate.dialect.MariaDBDialect